# Firebase Configuration
# These values are automatically provided in production via Pulumi outputs
# For local development, you can get these from the Firebase Console → Project Settings → Web App
VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id

# Optional: Use Firebase emulator in development (automatically detected when using DEV mode)
# VITE_USE_FIREBASE_EMULATOR=true
